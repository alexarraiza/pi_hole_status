name: pi_hole_status
description: A flutter pi.hole status checker

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^3.0.10
  retrofit: ^1.3.4+1

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: ^1.4.1+1
  build_runner: ^1.10.13

flutter:
  uses-material-design: true
